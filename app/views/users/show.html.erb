<html>
<body>
  
<% if @user.present? %> 
<%= pluralize(@user.items.count, 'item') %>
<% end %>


<%= render partial: 'items/item', collection: @items %>

<%= render 'items/form' %>
 
</body>
</html>
