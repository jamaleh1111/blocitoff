

<%= link_to "Delete", [item.user(current_user), item], method: :delete, class: 'glyphicon glyphicon-ok', remote: true %>
<html>
<body>
  
  <div class="container">
    <div class="row">
      <div class="col-md-6 col-md-offset-3">
        <div class="table-responsive">
          <table class="table table-bordered">
              <ol 
                <li><%= item.name %></li>
              </ol>
              <%= link_to "Delete", [item.user(current_user), item], method: :delete, class: 'glyphicon glyphicon-ok', remote: true %>
          </table>
        </div>
      </div>
    </div>
  </div>  

</body>
</html>
