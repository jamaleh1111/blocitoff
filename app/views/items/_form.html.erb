<%= form_for [@user, @user.items.new], remote: true do |f| %>
  <div class="form-group">
    <%= f.label :placeholder, 'Add an item' %>
    <%= f.text_area :body, rows: 8, class: 'form-control'  %>
    <%= f.submit "Add Item", class: 'btn btn-success btn-sm' %>
  </div>
<% end %>